{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "main",
    "description": "Allow the local main window to use the desktop application's existing commands.",
    "windows": ["main"],
    "permissions": ["allow-desktop-commands"]
}
